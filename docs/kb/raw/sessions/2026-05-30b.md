---
date: 2026-05-30
branch: distributed-integration
---

# Session Log — 2026-05-30 (afternoon)

Continued from the morning session (see `2026-05-30.md`). Phases 1 and 2 of edge-only distributed mode were already complete at start of this session.

## Work Completed

### Resolved deferred Phase 1 checklist item

The last unchecked Phase 1 item was "Change `register_with_orchestrator()` to connect to ecav.py's gRPC server instead of C++ server." Confirmed on inspection that no code change was needed. `register_with_orchestrator()` (`edge_process.py:467`) already uses `orchestrator_ip`/`orchestrator_port` from CLI args — not hardcoded to the C++ server address. In edge-only mode, the container is launched with `--orchestrator_port 50055`, pointing it at the Python `EdgeRegistrationServer`. After registration, `run()` checks `if not self.carla_ip:` and branches to the edge-only path, never calling any C++-only RPCs. Item marked `[x]`.

### Phase 3: `start_actors.sh` edge-only support (commit `d7bc9cb1`)

Added full edge-only mode support to `start_actors.sh`:

- New prompt: "Edge-only distributed mode?" → sets `mode_flag=-eo` (vs `-d`)
- ecav.py invocation uses `$mode_flag` instead of hardcoded `-d`
- Ready-signal wait is mode-aware:
  - Edge-only: waits for "EdgeRegistrationServer" in ECAV_LOG (server is up, safe to start edge containers)
  - Fully-distributed: unchanged "pushed scenario start" wait
- Edge containers in edge-only mode:
  - Port base 50060 (avoids collision with registration server on 50055 and C++ server on 50051)
  - Args: `--orchestrator_ip localhost --orchestrator_port 50055 -P <port>` (no `--edge-index`)
  - Wait signal: "edge-only ready" in container log (from `logger.info` in `edge_process.py:910`)
  - After all edges start: waits for `[EDGE-ONLY]` in ECAV_LOG (all fusion clients connected)
- Vehicle/RSU/non-ego containers: skipped entirely in edge-only mode (wrapped in `if not edge_only`)
- Health monitoring and summary output: mode-aware (skip non-edge container checks in edge-only)

## Files Changed

- `start_actors.sh` — 140 lines added, 55 removed
- `docs/agent_plans/edge_only_distributed_mode.md` — Phase 1 deferred item and Phase 3 script items marked `[x]`
- `docs/kb/wiki/current_state.md` — Phase 3 status updated; enumerated test steps added

## End-to-End Test (pending)

Not yet run. Steps when ready:

1. Start CARLA
2. Start edge container (terminal, no `-d`):
   ```
   docker run --runtime=nvidia --gpus all --network=host \
       --name=edge_0 -e HOSTNAME=edge_0 -e IS_DOCKER=1 \
       -v /opt/carla-simulator/PythonAPI:/opt/carla-simulator/PythonAPI:ro \
       ecav-python310:latest \
       python3.10 -u ecav/ecav2/edge_process.py \
           --orchestrator_ip localhost --orchestrator_port 50055 -P 50060
   ```
3. Run ecav.py: `python -u ecav.py -t openscenario_3_edge_worldfusion -v 0.9.15 -eo --apply_ml`

Success criteria: vehicle drives using edge-fused predictions; edge profiler JSON written; no crash in collect_features → fuse → apply_predictions loop.

---
<!-- append test results below when run -->
