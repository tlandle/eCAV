---
date: 2026-05-03
branch: distributed-integration
---

# Session Log — 2026-05-03

## Work Done

### Distributed container startup fixes (edge_process.py)
Three bugs fixed to get `openscenario_3_edge_worldfusion --apply_ml -d` running:
1. `_init_task` always-await guard — exceptions from phase A no longer silently swallowed
2. `CavWorld(apply_ml=False, config={'distributed': True})` in edge container — no YOLOv5, sets `run_distributed=True`
3. RSU `actor_id >= 0` guard — `world.get_actor(-1)` overflows unsigned int

All work committed in `787f4dac`.

### Regression matrix completions

| Test | Result |
|------|--------|
| 3: WF, no -l, -d | ✓ pipeline verified; collision expected (SMART maturity, Lincoln ~9 ticks) |
| 4: WF, -l, -d | ✓ identical to test 3; gRPC feature extraction confirmed via litserve |
| 5: LF, no -l, -d | ✓ no collision; SMART loaded; YOLO detections flowing |
| 8: LF, -l, -d | collision — SMART fires early on V2X beacon track, garbage predictions, behavior agent trusts exclusively |

### Late fusion self-detection regression — closed
Not a self-detection bug. Two compounding failures:
1. Wrong SMART checkpoint path (`ecav/core/prediction/...` → `models/smart/epoch=105.ckpt`)
2. No CUDA context → `EdgeProfiler._start_frame` crashed every tick → `run_step` returned nothing

### Fixes committed (96ab86c0)
- `edge_profiler.py`: probe CUDA device at init, gate `sample_gpu_utilization` on result
- `openscenario_3_edge_late_fusion.yaml`: correct SMART checkpoint path
- `edge_process.py`: n_preds counter handles list (late fusion) and dict (WorldFusion); features warning gates on both features AND objects being zero
- `openscenario_3_edge_worldfusion.py`: guard `edge.profiler.save_report()` for proxy edge managers

### AB3DMOT Kalman filter — NOT committed
An earlier session made a Joseph form covariance update change to `AB3DMOT_libs/model.py`. Reverted — change was based on wrong diagnosis (late fusion failures were the checkpoint + profiler bugs, not KF collapse). Tyler owns AB3DMOT; needs his review before changing.

## Key Architectural Finding: Late Fusion test 8 collision
SMART fires at tick 3 on the V2X beacon track (Lincoln stationary at spawn, -84.8, 80.0). Jitter buffer accumulates 23 ticks of beacon history quickly. SMART hallucinates 20 m/s for a stationary track and predicts the Lincoln teleporting 90m in 0.1s. Behavior agent trusts `edge_predictions` exclusively → misses real collision. Same research problem as WorldFusion SMART maturity. Tyler's problem to fix.

## Files Changed
- `ecav/ecav2/edge_process.py`
- `ecav/core/application/edge/edge_profiler.py`
- `ecav/scenario_testing/config_yaml/openscenario_3_edge_late_fusion.yaml`
- `ecav/scenario_testing/openscenario_3_edge_worldfusion.py`
- `docs/kb/wiki/current_state.md`
