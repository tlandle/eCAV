# Session: 2026-04-05 (second session)

## Branch
`distributed-integration`

## What We Did

### 1. Fixed start_actors.sh (four bugs)

- **LitServe path wrong**: `opencda/ml_manager/litserve_models.py` → `ecav/ml_manager/litserve_models.py` (file didn't exist at old path)
- **Base process entry point**: `opencda.py` → `ecav.py`
- **Ego vehicle containers**: `opencda.py` → `ecav.py`
- **Non-ego vehicle container**: `opencda.py` → `ecav.py`
- Stale comment updated to match

Investigation confirmed: RSUs using `ecloud_actor_client.py` and ego vehicles using `ecav.py` is intentional — ego vehicles run CARLA ScenarioRunner (`run_vehicle`), RSUs are pure gRPC actors. `litserve_models.py` starts both the WorldFusion HTTP server (port 18000) and YOLO gRPC server (port 18001) in one process — no distinction needed between scenarios.

### 2. Debugged and fixed ecloud_server build

- **Connection refused** at `Server_SetEdgeMappings`: ecloud_server binary was corrupted (missing section headers at 77306032)
- cmake was run from `ecav/ecloud_server/cmake/build` but Makefile was in `ecav/ecloud_server` (in-source build); `make` was failing because it ran in the wrong directory
- Fix: `cd ~/eCAV/ecav/ecloud_server && make -j4`
- Build succeeded; regenerated C++ proto stubs from `ecloud.proto`, produced clean binary

### 3. Fixed Docker build (two bugs)

**Bug 1 — torch_cluster build order:**
- `torch_cluster==1.6.3` imports torch during its `setup.py` build, but torch appeared after it in `requirements_3_10.txt`
- Also: `==1.6.3` pin is stale for torch 2.9.1; needs pre-built wheel from PyG index
- Fix in `requirements_3_10.txt`: removed version pins (`torch_cluster==1.6.3` → `torch_cluster`, `torch_scatter==2.1.2` → `torch_scatter`)
- Fix in `Dockerfile`: pre-install `torch==2.9.1 torchvision==0.24.1` before requirements, add `-f https://data.pyg.org/whl/torch-2.9.1+cu128.html` to requirements install

**Bug 2 — wrong BM2CP path:**
- Dockerfile referenced `opencda/BM2CP` (doesn't exist); correct path is `ecav/BM2CP` (populated submodule)
- Fixed both occurrences (`pip install -e` and `cd ... setup.py build_ext`)

## Files Changed

- `start_actors.sh` — 4 path/entry-point fixes
- `Dockerfile` — torch install order + BM2CP path
- `requirements_3_10.txt` — removed stale version pins on torch_cluster/torch_scatter

## Status

Docker build successful. ecloud_server rebuilt and functional. Ready for Phase 8 end-to-end test.
