# Session 2026-07-16 .. 2026-07-19 — score-head calibration + mode-sweep planner

Branch: paper-closed-loop-recreate

## What happened

Follow-up to the live MTR integration: measured whether the confidence head
actually selects the best mode, then upgraded the planner to consume the
mode set.

## Findings

- Score-head calibration (full test split, 1980 objects): oracle minADE6
  1.45 m vs score-selected ADE 2.33 m; top-1 agreement 62.6%, top-2 79%.
  Selected-ADE p90 5.78 m (tail = diverging-mode cases).
- Live delivered ADE ≈ offline selected-mode ADE → the live pipeline adds
  no degradation; mode selection is the dominant error source.
- Futures are dense (90% full 5 s; GT-sourced), refuting the truncation
  label-noise hypothesis; gap is intrinsic multimodality + thin pasts.
- ecloud.proto GeneratedTrajectory carries only one trajectory, but
  distributed:false runs are in-process, so mode data reaches the planner
  without proto changes.

## Changes

- `behavior_agent.py`: collision loop sweeps top-K modes by score
  (`prediction_mode_top_k`, default 2), earliest conflicting TTC wins;
  drawing re-run uses the conflicting mode; non-multimodal predictors
  unchanged. Precedent: Apollo per-trajectory ST boundaries, Autoware
  confidence-iterated predicted_paths.

## Validation

Smoke run 5: exit 0, zero tracebacks, FDE 0.77-1.20 m / MR 17-23% (same
band as argmax-only), zero ghost brakes / false brakes.

## Next

- Fast-Tesla rerun (mode divergence payoff case; background traffic was
  near-stopped so far).
- AB3DMOT ~9-tick fragmentation fix (deepens pasts → sharper mode scores).
- Live RELAY Q1/Q4/Q5/Q6 arms.
