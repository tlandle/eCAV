# 2026-08-09 (session block 08-08 → 08-09): proposal comment rounds, terminology, milestones

Paper/proposal session continuing 08-07; no sandbox code touched.
Repo: Dissertation_proposal (through commit 5ccee07, all pushed).

## Overleaf comment rounds merged

Three rounds: overleaf-2026-08-08-1904 branch (manual merge), a direct
origin/main push, and overleaf-2026-08-09-1741 (merged on GitHub side).
All comments addressed with `% \tl{... || FIXED: ...}` annotations;
zero active \tl remain.

## eCAV chapter

- "world-queried" → ground-truth state, defined at first use, swept
  document- and deck-wide (Tyler: use the official name).
- Related work rewritten in three comparative clusters (single-host
  closed-loop: OpenCDA/V2Xverse/AutoCast/Coopernaut/EI-Drive;
  network-in-the-loop: ms-van3t-CARLA/OpenCAMS; latency-first:
  Pylot/D3); stray paragraph dissolved; Multi-V2X/OPV2V/DAIR-V2X added
  to dataset list.
- Non-goals framing removed ("arguing with a ghost"); section is System
  Goals.
- Distributed execution expanded with the tick-barrier protocol and a
  new TikZ architecture figure (fig:ecav-arch). Two iterations on the
  figure per Tyler: components named by role not technology (physics
  and world engine, not CARLA; implementation mapping moved to the
  caption), RSU also feeds the shared perception service, edge box
  describes its function and is marked optional, sidelink direct
  exchange between vehicles and RSUs, radio plane fans to all actors.
- "capstone experiment" (invented term) → Platform Evaluation;
  "run ensembles" → repeated runs.
- Shared infra closes with the behavior argument: safety outcomes +
  traffic flow are the point; SUMO-style trace replay cannot measure
  intervention effects (sumo2018 bib entry added).

## paper1 (safety envelope)

- Self-ghosting demoted from "dominant" to "a frequent" logic failure
  in all three places (Tyler: recent work shows it is not dominant).
- Problem statement now points to fig:bg-arch for the boundary diagram.
- "not a bandwidth or compute limit" contrastive clause dropped.
- Anchoring cost figures scoped to late fusion + V2X2V cooperative
  prediction (not universal).

## paper3 (scale-up)

- Motivation lead-in: late vs early vs intermediate fusion walked from
  chap 4's apparatus, new boundary-comparison TikZ figure
  (fig:scaleup-boundaries), pipeline stages explained (prediction =
  per-object transformer, the heavy stage), per-ego V2V multiplication
  + so-good-it-is-bad admission argument.
- Bottleneck placed per boundary; measured uplink 17→36 ms (N=4→31);
  selection composes only with intermediate fusion.
- "CMP-shaped" gone: direct closed-loop comparison stated (CMP 0.48 vs
  controller 0.90).
- Evaluation Plan → Evaluation with published SEC numbers; Expected
  Contributions → Contributions (accepted paper).

## paper5 (scale-out)

- Motivation: boundary-before-conflict scenario figure
  (fig:scaleout-boundary) + overlap-cannot-substitute argument
  (bounded sensing, state lives at source, duplicate pipelines spend
  the chap 5 agent budget).
- Preliminary results: live two-edge full-stack runs added (right
  merge + blind overtake, WF + SSM tracker + MTR on both edges,
  forecast trigger ~4 s ahead, ~440 B/track latent memo, warm resume
  both directions, ego ownership handoff, zero collisions). Sourced
  from the 08-06 sandbox session results.

## Terminology and milestones (document-wide)

- "world model" retired everywhere (document + deck): shared state /
  tracks and motion forecasts / shared picture of surrounding traffic.
- "service continuity" → scale-out (chapter title, abstract, intro
  roadmap, timeline).
- "state consistency" (as direction name) → safety envelope.
- Milestones table + list reordered to document order with corrected
  venues: eCAV → ISPASS Dec 2026 (was MobiSys), safety envelope →
  SenSys 2027 under submission, scale-up → SEC 2026 accepted to
  appear, scale-out → NSDI Sep 2026, comm arch → SenSys or MobiSys
  fall deadline. Proposal itself removed from the schedule (document
  and deck); "this is a proposal, not a proposal defense".

## Build state

59 pages, zero undefined refs, zero active \tl. Deck 38 slides,
regenerated (thesis slide, schedule slide, ground-truth naming).

## External review round (same day, after the comment rounds)

Long technical review of the full proposal received; applied its priority
list in plain language (commit 3896307): four-conditions framing in the
research directions intro (identity consistency, freshness, continuity,
communication delivery); Ch2 taxonomy fixed (VI-Eye/VRF are early-fusion
I2V; topology and fusion level orthogonal; trade-off framing replaces
proof-by-elimination); Ch7 submodularity claim replaced with greedy
heuristic vs offline oracle + proxy calibration; Foresight migrates
"temporal context" not "recurrent hidden state" (MambaTrack claim
softened); commit decoupled from radio handover (locale crossing is the
trigger); 300 ms reconciled with the 220-450 ms envelope (operating
point, controller takes bound as input); risk score stated as global
(min closest approach over all tracked vehicles); eCAV frame-supersede
backlog semantics documented; CADET/AdaMap/V2X-Graph/Co-MTP/V2XPnP/
TOCOM-V2I added; SBA scoped to connected participants; delay-source
finding scoped to delivered/ordered/consistent state; NHTSA claim
narrowed; locale sizing uses controlled envelope; bib audit (V2X-Sim
entry was a Gadzicki copy, fixed; dup Trajectron++/SyncNet/CMP entries
removed and repointed). 61 pp, 0 undefined refs.

Deferred to Tyler: conditional thesis form vs Kishore's assertive form;
ptag removal (awaits Kishore); Ch2 seven-column matrix; talk restructure
(20 slides, 60% time on ch4-5); AoI-budget bridge diagram; SAC 2026
RSU-handover cite (metadata unverified, novelty scoping done via MoDEMS).

## Third review round (thesis-evidence alignment and mechanism fixes, 5fc937c)

- Thesis second half rewritten so every clause maps to a chapter: extends
  safe operation in occluded conflicts / maintains state within
  maneuver-specific freshness budgets / preserves service across mobility
  and communication constraints. TRAFFIC FLOW REMOVED from the thesis
  (not a demonstrated result; Kishore had asked for it — flagged to
  Tyler). Universal 300 ms and cooperative-planning-exhaustion clauses
  also removed. Deck slide + note synced; note includes the oral answer
  for the traffic-flow question.
- AoI margin parameterized: m_q = tau_max(u) - Q_q(Delta_use); ch4
  evaluates q=0.99, ch5 accepted evaluation q=0.95, both stated.
- eCAV timing rewritten as physical vs logical separation (invocation
  runs inside its barrier slot; logical occupancy derived from measured
  stamps; supersede semantics in logical time). Resolves the causality
  question about t+50/t+100 frames.
- Foresight: ownership epochs + authority token (at most one epoch
  publishable/consumable per track); runtime readiness condition
  separated from offline equivalence oracle; B3 reactive
  replicated-store fetch arm added (six arms); replication-without-
  inference design point argued in motivation; 440 B vs 1.3 KB payload
  points explained.
- Ch7: transmission unit defined (sidelink object records; Uu spatial
  feature tiles, utility summed over covered tracks); equation deleted
  in favor of the prose heuristic; constrained + unbounded oracles;
  global risk score aligned with ch5; NR Mode 2 sensing-based wording;
  Uu saturation regime language; tau_max(u) instead of flat 300 ms;
  chapter retitled ...Cooperative Perception and Prediction.
- Trust/assumptions paragraph in background (advisory service,
  authenticated substrate, session-stable identity); SBA attack-surface
  note in ch4; apparatus assumptions (clock sync, localization).
- Claim scoping: LOS "extends observability", late-fusion sub-threshold
  phrasing, selection-at-any-boundary, correlated-occlusion hedged,
  determinism defined, hundreds-of-agents as target, OpenCOOD/OPV2V
  naming, broader-impacts crash wording, real-scale → large-scale,
  fig 2.1 caption boundary levels, table 2.1 trade-off analysis.
- 62 pp, 0 undefined refs.
