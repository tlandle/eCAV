# Session: 2026-04-04 (second session)

## Branch
`distributed-integration`

## What We Did

### 1. Fixed worldfusion_litserve_plan.md numbering
- Renamed second "O5 — gRPC transport" → "O7 — gRPC transport" (matched checklist)
- Renamed "O4 — LiDAR-only mode" → "Variant — LiDAR-only mode" (avoided collision with O4=uint8)
- Fixed stale uppercase file reference `EDGE_ARCHITECTURE_PROPOSAL.md` → `edge_architecture_proposal.md`

### 2. Full edge architecture codebase audit
Swept all files relevant to Phases 0–7 of the edge architecture proposal. Findings:

**Complete:**
- Phase 1: `ecav/protos/ecloud.proto` — all edge messages + RPCs, plus bonus intermediate fusion messages
- Phase 3: `ecav/ecloud_server/ecloud_server.cc` — `Edge_Register`, `Edge_TickComplete`, `Client_GetConnectionInfo`, `Server_SetEdgeMappings`, edge state tracking (`vehicleToEdgeMapping_`, `edgeInfos_`, etc.)
- Phase 4: `ecav/ecav2/edge_process.py` — full async framework; `fuse_predictions()` is placeholder
- Phase 5: `ecav/ecav2/ecloud_actor_client.py` — `Client_GetConnectionInfo` call, edge routing, `Edge_ActorRegister`, `Edge_ActorSendUpdate` in tick loop
- Phase 7: `start_actors.sh` — edge containers launched before vehicles via `edge_process.py -e $e -P $edge_port`

**Not needed:**
- Phase 2: edge_index/port not in YAML — dynamic assignment is the actual design

**Partial:**
- Phase 0: `--litserve` flag done; `distributed:` YAML field still in ~75 files (inert)

**Blocking gap:**
- Phase 6: `sim_api.py` never calls `Server_SetEdgeMappings` → orchestrator's `vehicleToEdgeMapping_` empty → `Client_GetConnectionInfo` always returns `has_edge=false` → actors always connect to orchestrator directly

### 3. Updated plan docs
- `edge_architecture_proposal.md`: Phase status annotations in Implementation Order section
- `worldfusion_litserve_plan.md`: Distributed Architecture section rewritten with accurate status; Implementation Order updated
- `docs/kb/wiki/plans_index.md`: edge plan status updated
- `docs/kb/wiki/current_state.md`: edge architecture section rewritten
- `docs/kb/wiki/decisions.md`: Added D11 (dynamic edge assignment)

## Key Finding
The distributed edge architecture is ~95% built. The only thing preventing end-to-end operation is a missing `Server_SetEdgeMappings` call in `sim_api.py`. Once that's added (Phase 6), Phase 8 (build/test) becomes the next step.

## Next Steps
1. Implement Phase 6: add `compute_edge_mappings()` and `Server_SetEdgeMappings` call to `sim_api.py`
2. Phase 8: end-to-end test with `start_actors.sh`
3. WorldFusion O5: implement `_run_batched_encoder()` in `EdgeProcess.fuse_predictions()` (can be done in parallel with or after Phase 6/8)
