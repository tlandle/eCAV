# Session Log — 2026-07-27

**Branch:** `develop`  
**Commit:** `6eef5c89` (Scenario B validated run 11)

---

## What Was Done

### 1. TRACK-DBG diagnostic (`edge_manager_prediction_late_fusion_ab3dmot_linear_predictor.py`)

Added `_n_trk_before` capture before `self.tracker.track()` call and changed log level to WARNING with tag `[TRACK-DBG]`. Fields added: `trk_before=`, `trk_after=`, edge ID for disambiguation (needed because the diagnostic runs on both edges per tick).

**Finding:** At tick=183, edge 0 (6954b829) has `dets_in=0` while edge 1 (a1ee6cc3) already has 4 active tracks. This conclusively confirms hypothesis (a): NPC detections never reach edge 0's tracker because RSU0 has no NPC-lane coverage at that position (blind donut ±35m around x=75). Hypotheses (b) track-birth bug and (c) clock mismatch are eliminated.

### 2. Predictive obstacle handoff trigger (`openscenario_multi_edge_right_merge.py`)

`_build_locale_router` now returns a 3-tuple `(LocaleRouter, edge_by_locale, locale_by_id)` so `Locale` objects can be accessed directly for `predicted_to_exit_within` calls.

Added `OBSTACLE_HANDOFF_LOOKAHEAD_S = 1.0` constant at module level (20-tick horizon at world_dt=0.05s; at 18 m/s fires ~18m before the locale 0 boundary).

Obstacle handoff block restructured into two tiers:
- **PREDICTIVE TRIGGER**: each tick while NPC is in locale 0 and no handoff pending, builds constant-velocity trajectory using `npc_actor.get_velocity()`, checks `src_locale.predicted_to_exit_within(traj, 1.0, world_dt)`. If yes, fires `transfer_obstacle_state` immediately.
- **REACTIVE FALLBACK**: unchanged geometry-event path via `locale_tracker.update`.

Both tiers log diagnostics if `cost is None` (no track available yet on src_edge).

### 3. RSU0 position (`openscenario_multi_edge_right_merge.yaml`)

Moved x=75 → x=55. Rationale: at z=7m, near-field blind donut is ±35m. Moving to x=55 shifts reacquisition zone to approximately x=90-110, which covers the locale 0 boundary at x=115. YAML comment added explaining the geometry.

### 4. `collision_time_ahead: 2 → 4` fix

**Run-10 finding:** at `collision_time_ahead=2`, hazard fires at TTC<2s ≈ 22m from ambulance. Ego decelerates over ~12m and stops ~5m from ambulance rear. During the creep-and-merge phase, ego advanced to 3m clearance → CARLA physics registered a collision.

Fix: raised to 4, fires at TTC<4s ≈ 44m. This gives 30m of clearance when fully stopped, and ego can complete the lateral merge before reaching the ambulance's x-position.

YAML comment added with the run-10 rationale.

### 5. Curved-road suppression bug documented (plan only, not fixed)

`behavior_agent.py:1555-1556`:
```python
elif is_hazard and (not left_turn) and (not self.overtake_allowed or
        self.overtake_counter > 0 or self.get_local_planner().potential_curved_road):
    car_following_flag = True
```
The `potential_curved_road` term forces car-following even when `overtake_allowed=True` and `overtake_counter==0`. Fires when merge waypoints look curved to the local planner. Tyler flagged with `#TL — Why is this logic here?`. Added as OPEN item in `edge_handoff_phase1_state_transfer.md`. Fix: remove `potential_curved_road` from step-8 guard. Tyler scope; bring to sync.

### 6. Run 11 validation

Full log parse of `/home/jordan/eCAV/tmp_right_merge_handoff.txt` (35K lines):

| Event | Tick | Detail |
|---|---|---|
| NPC resolved | 31 | carla_id=244 |
| Vehicle handoff | 63 | vid=242, locale_0→locale_1, bytes=986, total_ms=88 |
| NPC enters RSU1 range | 243 | d=59.2m |
| First ambulance hazard | ~305 | dist=42.30m, ego_pos=(235.2, 136.8), track_id=1 |
| NPC passes ego | ~370-380 | track_id=61, TTC=0.00s, obs_pos=(279-284, 140.4) at 17 m/s |
| Overtake initiates | 425 | ego=(267.7, 137.1), ov_ctr=98 |
| Merge complete | 500 | ego=(278.8, 141.2), ambulance at (280, 136.9), 4.3m lateral clear |
| Ego en route | 500-695 | spd=10-11 m/s, hazard_flag=False, ttc=1000 |
| Scenario ends | 700 | MAX_STEP; ego at (384.1, 140.8), destination=(600, 139.5) |

**Zero "Safety Warning from the safety manager" in 700 ticks.**

The track_id=76 PRED COLLISION cluster (TTC min=0.39s, dist=2.7m) at ticks ~475-490 is the tracker measuring the ambulance's KF corner (KF at 277.1 vs actual 280) during the lateral merge. TTC nadir then increases as ego clears. No CARLA physics collision.

Scenario terminates at MAX_STEP=700 with ego at x=384, destination x=600. The remaining 216m of straight cruising is outside the research sequence (detection → wait → merge).

---

## Files Changed

- `ecav/core/application/edge/edge_manager/edge_manager_prediction_late_fusion_ab3dmot_linear_predictor.py` — TRACK-DBG diagnostic
- `ecav/scenario_testing/openscenario_multi_edge_right_merge.py` — predictive trigger + locale_by_id
- `ecav/scenario_testing/config_yaml/openscenario_multi_edge_right_merge.yaml` — RSU0 x=55, collision_time_ahead=4
- `docs/agent_plans/edge_handoff_phase1_state_transfer.md` — curved-road bug OPEN note, run-10 collision_time_ahead checklist item

---

## Open Items

1. **NPC obstacle KF handoff** — predictive trigger fires correctly but `transfer_obstacle_state` returns `None` (edge 0 has zero NPC tracks). Root cause: RSU0 blind donut persists even at x=55; NPC transits reacquisition zone faster than AB3DMOT can confirm a track (needs 3 confirmed hits at `min_hits`). Deeper hypothesis: after ego handoff at tick=63, `vehicle_manager_list` on edge 0 becomes empty — RSU detection processing may gate on non-empty vehicle list. Check `edge_manager_prediction_late_fusion_ab3dmot_linear_predictor.py` RSU detection path for this guard.

2. **Diagnostics cleanup** — `[TRACK-DBG]` / `[EGO-DBG]` / `[SCENB-DBG]` at WARNING level. Remove after obstacle path resolves.

3. **Scenario MAX_STEP** — ego doesn't reach x=600 within 700 ticks. If we want to demonstrate full route completion, increase MAX_STEP or reduce destination distance.

4. **Tyler sync** — curved-road bug at `behavior_agent.py:1555`, track-birth findings, predictive trigger as Phase 2 primitive, Phase 2 `-eo` distribution plan.

5. **Docker cleanup** — `docker system df` showed build cache ~80GB with ~41GB reclaimable. Run `docker system prune` when convenient.
