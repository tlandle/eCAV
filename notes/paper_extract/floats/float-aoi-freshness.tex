\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/aoi_freshness.pdf}
\caption{(a) Planner-side staleness $\Delta_{\text{use}}^{\text{meas}}$ scales linearly with configured latency and dominates total AoI. (b) Model freshness $\Delta_{\text{gen}}$ is bounded by the edge update loop. Together, these justify indexing the envelope by $p99(\Delta_{\text{use}})$ rather than by nominal configured delay.}
\label{fig:aoi_freshness}
\end{figure}
