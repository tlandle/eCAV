\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/first_failure.pdf}
\caption{First-failure categorization (focal ego) for $N \in \{1,2,4,8,16\}$, SBA off (top) and SBA on (bottom). At low latency most runs succeed. As latency increases, multi-source stacks transition to self-ghosting as the dominant first failure (SBA off), while Oracle transitions to physics-limited failures. With SBA, ghost bars shrink and failures shift toward physics and progress limits.}
\label{fig:first_failure}
\end{figure*}
